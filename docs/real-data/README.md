# Stage 6 真机接入准备资料

## 文件说明

- `1.jpg`：当前系统通讯/集成草图，用于说明机器人、视觉控制器、HMI、AI 控制器、相机、焊机等现场系统之间的初步关系。
- `2.jpg`：当前可导出的机器人、焊接、相机、工艺、任务字段盘点，用于梳理 Stage 6 真机接入准备阶段的字段来源和待确认范围。

## 当前解读

这两张图片是 Stage 6 规划证据，用来支撑“真机数据接入与数据资产化试点”的阶段判断。它们可以帮助团队理解现场数据链路、主要设备对象、字段类别和后续沟通重点。

当前解读仅限于规划层面：`1.jpg` 说明系统通讯/集成关系，`2.jpg` 说明可导出字段的初步盘点。它们不是生产协议文档，也不是最终工程接口说明。

## 使用边界

不能从截图直接推断精确 payload schema、采样频率、数据库 schema 或 connector 实现。

在缺少真实 SDK/TCP JSON 示例、文件样例、数据库表结构和现场时间基准前，不应据此新增生产 connector、固定数据库设计或修改 Physical AI Package schema。

## 后续需要补充

- SDK/TCP JSON 示例。
- 文件命名规则。
- 如使用 DB，则提供表结构。
- 时间戳来源。
- 字段单位。
- 坐标系。
- 存储路径。
- 权限和脱敏规则。
